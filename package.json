{
  "name": "json-formatter",
  "version": "1.0.0",
  "description": "JSON formatter, validator, and minifier",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
